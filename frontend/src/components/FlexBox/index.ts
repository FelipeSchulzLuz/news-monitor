import styled from 'styled-components';
import Box from '@mui/material/Box';

/**
 * FlexBox é um componente base que já vem com display: flex,
 * podendo ser extendido para criar novos containers flex.
 */
export const FlexBox = styled(Box).attrs({
    display: 'flex',
})``;
